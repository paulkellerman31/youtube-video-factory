# Copy to .env and fill. .env is gitignored — never commit it.
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
# FAL_KEY=        # v2 only (Kling)

# --- Modèle image (A/B sans toucher au code) ---
# Défaut prod = gpt-image-1. Pour tester le nouveau : IMAGE_MODEL=gpt-image-1.5
# Voir MODEL_NOTES.md (paliers qualité à vérifier + tarif).
# IMAGE_MODEL=gpt-image-1
# IMAGE_QUALITY=medium   # low | medium | high (corps-humain = high)
