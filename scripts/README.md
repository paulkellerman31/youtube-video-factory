# scripts/ — la couche "mains"

NE PAS ecrire ces scripts a la main. Ils se construisent dans **Claude Code**, a partir de
`../BUILD-SPEC.md` (voir la requete du paragraphe 9). Contenu attendu (v1 spine) :
generate-audio.ts, generate-images.ts, assemble.ts, lib/, cli.ts.
